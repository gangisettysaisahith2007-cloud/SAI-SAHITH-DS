#include <stdio.h>
int main() {
    int n,i,a[50];
    scanf("%d",&n);
    for(i=0;i<n;i++) scanf("%d",&a[i]);
    for(i=0;i<n;i++)
        if(a[i]%2==0) printf("%d Even\n",a[i]);
        else printf("%d Odd\n",a[i]);
}
